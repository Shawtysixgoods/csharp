## 1. Классы и объекты. Поля и методы

**Теоретическая часть:** Что такое класс и что такое объект в C#? В чём между ними разница?

**Практическая часть:** Создайте класс `Dog` с полями `name` (имя) и `age` (возраст). Добавьте метод `Bark()`, который выводит в консоль строку «Гав-гав!». В методе `Main` создайте объект класса и вызовите метод.

---

## 2. Конструктор класса

**Теоретическая часть:** Что такое конструктор класса и для чего он нужен?

**Практическая часть:** Создайте класс `Book` с полями `title` и `author`. Напишите конструктор, который принимает два параметра и инициализирует поля. Создайте два объекта книги и выведите их данные.

---

## 3. Свойства (аксессоры)

**Теоретическая часть:** Чем свойства (properties) отличаются от обычных полей класса? Какие ключевые слова используются для чтения и записи?

**Практическая часть:** Создайте класс `Student` со свойствами `Name` и `Grade` (оценка). Используйте автоматические свойства `{ get; set; }`. Создайте объект и выведите его данные.

---

## 4. Первичный конструктор

**Теоретическая часть:** Что такое первичный конструктор (primary constructor) в C# 12? В чём его удобство?

**Практическая часть:** Перепишите класс `Person(string name, int age)` с использованием первичного конструктора. Добавьте метод `Introduce()`, который выводит «Меня зовут {name}, мне {age} лет».

---

## 5. Инкапсуляция

**Теоретическая часть:** Что такое инкапсуляция в ООП и для чего она нужна?

**Практическая часть:** Создайте класс `BankAccount` с приватным полем `balance`. Реализуйте методы `Deposit(decimal amount)` и `Withdraw(decimal amount)`, которые проверяют корректность суммы. Баланс нельзя изменять напрямую извне.

---

## 6. Модификаторы доступа

**Теоретическая часть:** Перечислите основные модификаторы доступа в C# (`public`, `private`, `protected`, `internal`) и кратко объясните каждый.

**Практическая часть:** Создайте класс `User` с приватным полем `password` и публичным свойством `Login`. Реализуйте метод `CheckPassword(string input)`, который возвращает `true`, если пароль совпадает.

---

## 7. Наследование

**Теоретическая часть:** Что такое наследование в ООП? Какой символ используется в C# для обозначения наследования?

**Практическая часть:** Создайте базовый класс `Animal` с методом `Eat()`. Создайте производный класс `Cat`, наследуемый от `Animal`, и добавьте метод `Meow()`. В `Main` создайте объект `Cat` и вызовите оба метода.

---

## 8. Вызов конструктора базового класса

**Теоретическая часть:** Как в производном классе вызвать конструктор базового класса? Для чего это нужно?

**Практическая часть:** Создайте класс `Vehicle` с полем `speed` и конструктором. Создайте класс `Car`, наследуемый от `Vehicle`, с дополнительным полем `brand`. В конструкторе `Car` вызовите конструктор базового класса через `base(...)`.

---

## 9. Полиморфизм. Виртуальные методы

**Теоретическая часть:** Что такое полиморфизм? Какие ключевые слова в C# используются для переопределения методов?

**Практическая часть:** Создайте класс `Shape` с виртуальным методом `Area()`, возвращающим 0. Создайте производные классы `Circle` и `Square`, которые переопределяют метод `Area()` и возвращают свою площадь.

---

## 10. Ключевое слово `override`

**Теоретическая часть:** В чём разница между ключевыми словами `virtual`, `override` и `new` при работе с методами?

**Практическая часть:** Создайте класс `Employee` с методом `virtual void Work()`. Создайте класс `Programmer`, который переопределяет этот метод через `override` и выводит «Пишу код».

---

## 11. Абстрактные классы

**Теоретическая часть:** Что такое абстрактный класс? Можно ли создать объект абстрактного класса?

**Практическая часть:** Создайте абстрактный класс `Figure` с абстрактным методом `GetPerimeter()`. Создайте классы `Triangle` и `Rectangle`, которые реализуют этот метод.

---

## 12. Интерфейсы

**Теоретическая часть:** Что такое интерфейс в C#? Чем он отличается от абстрактного класса?

**Практическая часть:** Создайте интерфейс `IPrintable` с методом `Print()`. Реализуйте его в классах `Document` и `Photo`. В методе `Main` вызовите `Print()` у обоих объектов.

---

## 13. Множественная реализация интерфейсов

**Теоретическая часть:** Может ли класс в C# наследоваться от нескольких классов? А реализовывать несколько интерфейсов?

**Практическая часть:** Создайте интерфейсы `IReadable` (метод `Read()`) и `IWritable` (метод `Write()`). Создайте класс `Notebook`, реализующий оба интерфейса.

---

## 14. Структуры

**Теоретическая часть:** Что такое структура (struct) в C#? Чем она принципиально отличается от класса?

**Практическая часть:** Создайте структуру `Point` с полями `X` и `Y`. Добавьте метод `Show()`, выводящий координаты. Создайте две точки и выведите их.

---

## 15. Ссылочные и значимые типы

**Теоретическая часть:** Чем отличаются ссылочные типы данных (reference types) от значимых (value types)? Приведите примеры каждого.

**Практическая часть:** Создайте класс `RefBox` и структуру `ValBox`, у каждого — поле `Value`. В `Main` создайте объект каждого, передайте их в метод, изменяющий `Value`, и покажите разницу в поведении.

---

## 16. Классы vs структуры

**Теоретическая часть:** В каких случаях предпочтительнее использовать структуру, а в каких — класс?

**Практическая часть:** Создайте структуру `Coordinate` (X, Y) и класс `Player` с полем `Coordinate position`. Реализуйте метод `Move(int dx, int dy)`, изменяющий позицию игрока.

---

## 17. Построение иерархии классов

**Теоретическая часть:** Что такое иерархия классов? Какой класс в C# является корнем всей иерархии типов?

**Практическая часть:** Постройте иерархию: `Transport` → `LandTransport` → `Car`, `Bike`. У каждого класса добавьте поле или метод, характерный именно для него.

---

## 18. Конструктор с параметрами по умолчанию

**Теоретическая часть:** Что такое параметры по умолчанию в конструкторе и как они объявляются в C#?

**Практическая часть:** Создайте класс `Product` с полями `Name` и `Price`. Напишите конструктор, где `Price` имеет значение по умолчанию `0`. Создайте два объекта: один с указанием цены, другой — без неё.

---

## 19. Несколько конструкторов в классе

**Теоретическая часть:** Может ли класс иметь несколько конструкторов? По каким признакам они различаются?

**Практическая часть:** Создайте класс `Rectangle` с полями `Width` и `Height`. Напишите два конструктора: один без параметров (создаёт квадрат 1×1) и один с двумя параметрами. Создайте оба варианта объектов и выведите их размеры.

---

## 20. Перегрузка методов

**Теоретическая часть:** Что такое перегрузка методов (overloading)? По каким признакам компилятор различает перегруженные методы?

**Практическая часть:** Создайте класс `MathHelper` с тремя методами `Sum`: для двух `int`, для трёх `int` и для двух `double`. Проверьте их работу в `Main`.

---

## 21. Свойства с валидацией

**Теоретическая часть:** Как в сеттере свойства реализовать проверку на корректность присваиваемого значения?

**Практическая часть:** Создайте класс `Person` со свойством `Age`. В сеттере запретите устанавливать значение меньше 0 и больше 120.

---

## 22. Интерфейс с несколькими методами

**Теоретическая часть:** Может ли интерфейс содержать несколько методов? Обязан ли класс реализовать их все?

**Практическая часть:** Создайте интерфейс `IPlayer` с методами `Play()`, `Pause()` и `Stop()`. Создайте класс `MusicPlayer`, реализующий все три метода, и вызовите их в `Main`.

---

## 23. Приведение типов (upcasting/downcasting)

**Теоретическая часть:** Что такое восходящее и нисходящее приведение типов? Для чего используются операторы `is` и `as`?

**Практическая часть:** Создайте класс `Shape` и производные классы `Circle` (с методом `DrawCircle()`) и `Square` (с методом `DrawSquare()`). Поместите объекты в массив `Shape[]` и в цикле через `is` определите реальный тип каждого объекта, чтобы вызвать соответствующий метод рисования.

---

## 24. Композиция классов

**Теоретическая часть:** Что такое композиция классов и чем она отличается от наследования?

**Практическая часть:** Создайте класс `Processor` (свойства `Model`, `Cores`) и класс `Computer`, содержащий поле типа `Processor`. Реализуйте в `Computer` метод `ShowSpecs()`, который выводит информацию о компьютере вместе с данными процессора.

---

## 25. Итоговое задание по ООП

**Теоретическая часть:** Перечислите 4 основных принципа ООП и кратко объясните каждый.

**Практическая часть:** Разработайте мини-систему «Зоопарк»:
- Абстрактный класс `Animal` со свойствами `Name`, `Age` и абстрактным методом `MakeSound()`.
- Классы `Lion` и `Parrot`, наследующие `Animal` и реализующие `MakeSound()` по-своему.
- Интерфейс `IFlyable` с методом `Fly()`, реализованный в классе `Parrot`.
- В `Main` создайте список `List<Animal>`, добавьте несколько животных и в цикле вызовите `MakeSound()`. Для тех, кто реализует `IFlyable`, дополнительно вызовите `Fly()`.
