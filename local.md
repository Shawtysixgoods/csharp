## Теоретические Вопросы

***

**Вопрос**
**Тема:** Ссылочные и значимые типы данных
**Вопрос:** Что такое значимые и ссылочные типы данных в C#? Приведите примеры каждого. Где в памяти хранятся значимые типы, а где — ссылочные? Что происходит при присваивании переменной каждого из типов другой переменной?

***

**Вопрос**
**Тема:** Ссылочные и значимые типы данных
**Вопрос:** Что такое boxing и unboxing в C#? Зачем это нужно? Приведите пример кода, в котором происходит упаковка (boxing) значимого типа `int` в тип `object` и последующая распаковка. Каковы возможные риски при unboxing?

***

**Вопрос**
**Тема:** Классы и структуры
**Вопрос:** В чём принципиальное отличие класса (`class`) от структуры (`struct`) в C#? Опишите отличия по следующим критериям: тип данных (ссылочный/значимый), наследование, конструктор по умолчанию, размещение в памяти. Когда предпочтительнее использовать структуру?

***

**Вопрос**
**Тема:** Классы и структуры
**Вопрос:** Что такое конструктор класса? Какие виды конструкторов существуют в C#? Что такое деструктор и финализатор? Как работает цепочка вызовов конструкторов через `: this()`?

***

**Вопрос**
**Тема:** Интерфейсы
**Вопрос:** Что такое интерфейс в C#? Какие члены может содержать интерфейс (начиная с C# 8.0)? Чем интерфейс отличается от абстрактного класса? Для чего используется явная реализация интерфейса?

***

**Вопрос**
**Тема:** Интерфейсы
**Вопрос:** Что такое множественная реализация интерфейсов? Почему C# запрещает множественное наследование классов, но разрешает реализацию нескольких интерфейсов? Как разрешается конфликт имён, если два интерфейса объявляют метод с одинаковой сигнатурой?

***

**Вопрос**
**Тема:** Абстрактные классы
**Вопрос:** Что такое абстрактный класс в C#? Какие ограничения на него накладываются (создание объекта, наследование)? Что такое абстрактный метод? Обязан ли дочерний класс переопределять все абстрактные методы родителя?

***

**Вопрос**
**Тема:** Абстрактные классы
**Вопрос:** Сравните абстрактный класс и интерфейс по следующим критериям: наличие реализации методов, поля и состояние, конструкторы, модификаторы доступа членов, множественное наследование. Когда следует выбирать абстрактный класс, а когда интерфейс?

***

**Вопрос**
**Тема:** Делегаты
**Вопрос:** Что такое делегат в C#? Как объявляется и используется делегат? Что такое многоадресный (multicast) делегат? Чем отличаются встроенные обобщённые делегаты `Action`, `Func` и `Predicate`?

***

**Вопрос**
**Тема:** События
**Вопрос:** Что такое событие (`event`) в C#? Как оно связано с делегатами? Опишите паттерн подписки и отписки от события (`+=`, `-=`). Что такое аксессоры события `add` и `remove`? Почему поле-событие объявляется с модификатором `event`, а не просто как делегат?

***

**Вопрос**
**Тема:** Наследование
**Вопрос:** Что такое наследование в ООП? Как реализуется наследование в C#? Что наследует дочерний класс от родителя, а что — нет (конструкторы, приватные поля)? Как вызвать конструктор базового класса через `base()`?

***

**Вопрос**
**Тема:** Наследование
**Вопрос:** Для чего используются модификаторы `virtual`, `override` и `new` при наследовании в C#? В чём разница между `override` и `new`? Что такое `sealed` класс и `sealed` метод? Приведите пример использования каждого модификатора.

***

**Вопрос**
**Тема:** Инкапсуляция
**Вопрос:** Что такое инкапсуляция? Перечислите модификаторы доступа в C# (`public`, `private`, `protected`, `internal`, `protected internal`, `private protected`) и опишите область видимости каждого. Почему поля класса обычно делают приватными?

***

**Вопрос**
**Тема:** Инкапсуляция. Свойства и аксессоры
**Вопрос:** Что такое свойство (`property`) в C#? Чем свойство отличается от поля? Что такое аксессоры `get` и `set`? Что такое автоматически реализуемые свойства (auto-properties)? Что означает `init` аксессор (C# 9.0)? Как реализовать свойство только для чтения?

***

**Вопрос**
**Тема:** Полиморфизм
**Вопрос:** Что такое полиморфизм? Какие виды полиморфизма существуют в C# (статический и динамический)? Что такое перегрузка методов (overloading)? Приведите пример статического полиморфизма через перегрузку методов и операторов.

***

**Вопрос**
**Тема:** Полиморфизм
**Вопрос:** Что такое динамический (runtime) полиморфизм в C#? Как он реализуется через виртуальные методы и переопределение? Как работает таблица виртуальных методов (vtable)? Что произойдёт, если вызвать виртуальный метод из конструктора базового класса?

***

**Вопрос**
**Тема:** Работа с файловой системой
**Вопрос:** Какие классы предоставляет пространство имён `System.IO` для работы с файловой системой? Опишите отличия между классами `File` и `FileInfo`, `Directory` и `DirectoryInfo`. Какой подход (статические методы vs экземплярные) предпочтительнее при многократных операциях с одним файлом?

***

**Вопрос**
**Тема:** Работа с файловой системой
**Вопрос:** Что такое поток (`Stream`) в C#? Как работают классы `StreamReader` и `StreamWriter`? Что такое `FileStream`? Зачем использовать конструкцию `using` при работе с потоками? Чем отличается чтение всего файла методом `File.ReadAllText()` от построчного чтения через `StreamReader`?

***

**Вопрос**
**Тема:** Делегаты. Лямбда-выражения
**Вопрос:** Что такое анонимный метод и лямбда-выражение в C#? Как лямбда-выражение связано с делегатами? Что такое замыкание (closure)? Приведите пример лямбды, захватывающей переменную из внешнего контекста, и объясните механизм замыкания.

***

**Вопрос**
**Тема:** Обобщение тем (сводный билет)
**Вопрос:** Перечислите и кратко охарактеризуйте четыре основных принципа ООП: инкапсуляцию, наследование, полиморфизм, абстракцию. Как каждый из них реализован в C#? Приведите по одному ключевому механизму языка (ключевому слову или конструкции) для каждого принципа.

***

***

## Практические Задачи 

***

**Задача**
**Тема:** Ссылочные и значимые типы
**Задача:** Создайте структуру `Point` с полями `X` и `Y` типа `double`. Создайте класс `PointRef` с аналогичными полями. Напишите программу, которая:
1. Создаёт два объекта `Point` (p1 и p2), присваивает p2 = p1, изменяет p2.X и выводит оба значения.
2. Делает то же самое для `PointRef`.

***

**Задача**
**Тема:** Классы и структуры
**Задача:** Создайте класс `BankAccount` со следующими характеристиками:
- Приватное поле `balance` типа `decimal`.
- Публичное свойство `Balance` только для чтения.
- Конструктор, принимающий начальный баланс (с проверкой: баланс не может быть отрицательным, иначе выбрасывается `ArgumentException`).
- Методы `Deposit(decimal amount)` и `Withdraw(decimal amount)` с валидацией суммы.

Продемонстрируйте работу класса в `Main`.

***

**Задача**
**Тема:** Инкапсуляция. Свойства
**Задача:** Создайте класс `Student` со свойствами:
- `Name` (строка, не может быть пустой или null).
- `Age` (целое, от 16 до 99, иначе `ArgumentOutOfRangeException`).
- `GPA` (double, от 0.0 до 5.0).
- Вычисляемое свойство `IsExcellent` (только get), возвращающее `true`, если GPA >= 4.5.

Напишите класс и продемонстрируйте корректную и некорректную инициализацию.

***

**Задача**
**Тема:** Наследование
**Задача:** Создайте иерархию классов:
- Базовый класс `Animal` с полями `Name`, `Age` и виртуальным методом `Speak()`, возвращающим строку.
- Классы `Dog`, `Cat`, `Parrot`, наследующие `Animal`, каждый из которых переопределяет `Speak()` своим звуком.
- В `Main` создайте список `List<Animal>`, добавьте туда объекты всех трёх типов и выведите имя и звук каждого животного в цикле.

***

**Задача**
**Тема:** Абстрактные классы
**Задача:** Создайте абстрактный класс `Shape` с:
- Абстрактными методами `Area()` и `Perimeter()`, возвращающими `double`.
- Виртуальным методом `Describe()`, выводящим тип фигуры, площадь и периметр.

Реализуйте наследников: `Circle` (радиус), `Rectangle` (ширина, высота), `Triangle` (три стороны). Создайте массив `Shape[]` и вызовите `Describe()` для каждого элемента.

***

**Задача**
**Тема:** Интерфейсы
**Задача:** Объявите интерфейс `IResizable` с методом `Resize(double factor)` и интерфейс `IDrawable` с методом `Draw()`. Создайте класс `Square`, реализующий оба интерфейса:
- `Resize` увеличивает сторону квадрата на переданный коэффициент.
- `Draw` выводит в консоль символьное представление квадрата (например, `##`, `##`).

Продемонстрируйте полиморфное использование через ссылки типа `IResizable` и `IDrawable`.

***

**Задача**
**Тема:** Интерфейсы. IComparable
**Задача:** Создайте класс `Product` со свойствами `Name` и `Price`. Реализуйте интерфейс `IComparable<Product>` для сортировки по цене. Дополнительно реализуйте `IFormattable` или переопределите `ToString()`, чтобы выводить продукт в виде `"Name: {name}, Price: {price:C}"`. Создайте список из 5 продуктов, отсортируйте через `List.Sort()` и выведите результат.

***

**Задача**
**Тема:** Абстрактные классы и интерфейсы (совместно)
**Задача:** Объявите интерфейс `IPayable` с методом `decimal CalculatePayment()`. Создайте абстрактный класс `Employee` с полями `Name` и `HoursWorked`, реализующий `IPayable`. Создайте конкретные классы `FullTimeEmployee` (фиксированная зарплата) и `PartTimeEmployee` (почасовая ставка), реализующие `CalculatePayment()` по-своему. Выведите платёжную ведомость для списка сотрудников.

***

**Задача**
**Тема:** Делегаты
**Задача:** Объявите делегат `MathOperation(double a, double b)`, возвращающий `double`. Напишите методы `Add`, `Subtract`, `Multiply`, `Divide`. Создайте массив делегатов, по одному на каждую операцию. Напишите метод `ExecuteAndPrint(MathOperation op, double a, double b)`, выполняющий делегат и выводящий результат. Продемонстрируйте вызов всех четырёх операций через цикл.

***

**Задача**
**Тема:** Делегаты. Func и Action
**Задача:** Напишите обобщённый метод `Filter<T>(List<T> list, Func<T, bool> predicate)`, возвращающий новый список, отфильтрованный по предикату. Напишите метод `Transform<T, TResult>(List<T> list, Func<T, TResult> selector)`, преобразующий каждый элемент. Примените оба метода к списку целых чисел: отфильтруйте чётные, затем преобразуйте в строки вида `"Число: N"`.

***

**Задача**
**Тема:** События
**Задача:** Создайте класс `Thermostat` с:
- Свойством `Temperature` (double).
- Событием `TemperatureChanged`, вызываемым при изменении температуры.
- Событием `OverheatWarning`, вызываемым, если температура превышает 100°C.

Создайте класс `TemperatureMonitor`, который подписывается на оба события и выводит соответствующие сообщения. Симулируйте изменение температуры в `Main`.

***

**Задача**
**Тема:** Полиморфизм. Перегрузка операторов
**Задача:** Создайте класс `Vector2D` с полями `X` и `Y`. Перегрузите операторы:
- `+` (сложение двух векторов),
- `-` (разность),
- `*` (умножение вектора на скаляр `double`),
- `==` и `!=` (сравнение),
- Переопределите `ToString()` в формате `"(X; Y)"`.

Продемонстрируйте все операторы в `Main`.

***

**Задача**
**Тема:** Полиморфизм. Virtual / Override
**Задача:** Создайте цепочку наследования: `Vehicle` → `Car` → `ElectricCar`. В каждом классе объявите виртуальный/переопределённый метод `GetInfo()`, возвращающий строку с информацией об объекте (каждый класс добавляет свои данные к строке родителя через `base.GetInfo()`). Продемонстрируйте вызов метода через ссылку типа `Vehicle` для объекта `ElectricCar`.

***

**Задача**
**Тема:** Инкапсуляция. Индексаторы
**Задача:** Создайте класс `Matrix`, представляющий двумерную матрицу `double[,]`. Реализуйте:
- Конструктор `Matrix(int rows, int cols)`.
- Индексатор `this[int row, int col]` с проверкой границ.
- Метод `Print()` для вывода матрицы в консоль.
- Перегрузку оператора `+` для сложения двух матриц одинакового размера.

***

**Задача**
**Тема:** Работа с файловой системой
**Задача:** Напишите программу, которая:
1. Создаёт директорию `StudentData` в текущей папке (если не существует).
2. Записывает в файл `students.txt` список из 5 студентов (имя, возраст, оценка) — по одному на строку.
3. Считывает файл построчно и выводит в консоль только тех студентов, у которых оценка выше 4.0.
4. Подсчитывает количество строк в файле и выводит его.

***

**Задача**
**Тема:** Работа с файловой системой
**Задача:** Создайте класс `Logger` с:
- Путём к файлу лога, задаваемым в конструкторе.
- Методом `Log(string level, string message)`, дописывающим в файл строку вида `[2026-03-17 12:00:00] [INFO] Сообщение`.
- Методами `LogInfo`, `LogWarning`, `LogError` как обёртками над `Log`.
- Методом `GetLastNLines(int n)`, возвращающим последние N строк файла.

Продемонстрируйте работу логгера в `Main`.

***

**Задача**
**Тема:** Наследование и полиморфизм (совместно)
**Задача:** Создайте систему «Зоопарк»:
- Абстрактный класс `Animal` с абстрактным методом `MakeSound()` и виртуальным `Move()`.
- Интерфейс `ISwimmable` с методом `Swim()`, `IFlyable` с методом `Fly()`.
- Классы: `Duck` (Animal + ISwimmable + IFlyable), `Fish` (Animal + ISwimmable), `Eagle` (Animal + IFlyable).

В `Main` создайте список `List<Animal>`, добавьте по одному объекту каждого типа. В цикле выводите звук, и, через проверку `is`/`as`, вызывайте `Swim()` или `Fly()` если доступно.

***

**Задача**
**Тема:** Делегаты и события (совместно)
**Задача:** Реализуйте упрощённую систему уведомлений:
- Класс `Button` с событием `Clicked` (используйте `EventHandler<EventArgs>`).
- Класс `TextBox` с событием `TextChanged` (используйте `EventHandler<string>`).
- Класс `Form`, который создаёт кнопку и текстовое поле, подписывается на их события и выводит уведомления.

Симулируйте нажатие кнопки и изменение текста в `Main`.

***

**Задача**
**Тема:** Интерфейс IDisposable. Работа с ресурсами
**Задача:** Создайте класс `FileProcessor`, реализующий интерфейс `IDisposable`:
- Конструктор открывает `StreamWriter` для записи в указанный файл.
- Метод `WriteLine(string text)` записывает строку в файл.
- Метод `Dispose()` корректно закрывает поток и выводит `"Ресурсы освобождены"`.

Продемонстрируйте использование через `using` и убедитесь, что файл содержит записанные строки после выполнения.

***

**Задача**
**Тема:** Обобщённый (сводный) билет
**Задача:** Разработайте мини-приложение «Библиотека»:
- Абстрактный класс `LibraryItem` (Title, Author, Year) с абстрактным методом `GetInfo()`.
- Классы `Book` (ISBN, количество страниц) и `Magazine` (номер, месяц), наследующие `LibraryItem`.
- Интерфейс `IBorrowable` с методами `Borrow(string userName)` и `Return()` и свойством `IsAvailable`.
- Класс `Catalog` с событием `ItemBorrowed` и методами `Add`, `FindByAuthor` (через `Func<LibraryItem, bool>`).
- Сохранение каталога в текстовый файл `catalog.txt` и чтение из него.
